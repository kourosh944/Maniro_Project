# Maniro backend — وابستگی‌های پایتون
#
# این فایل برای سازگاری با روش قدیمی نصب باقی مانده و پیش‌فرض همان
# وابستگی‌های development را نصب می‌کند. برای انتخاب دقیق‌تر محیط:
#
#   pip install -r requirements/development.txt   # برای development محلی
#   pip install -r requirements/production.txt     # برای سرور production
#
-r requirements/development.txt
